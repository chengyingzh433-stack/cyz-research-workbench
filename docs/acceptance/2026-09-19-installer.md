# Windows 安装版验收（2026-09-19）

## 本地产物

- 文件：`release/CYZ-Research-Workbench-0.1.0-Setup-x64.exe`
- 平台：Windows x64；Electron 44.4.3、electron-builder 26.15.3；NSIS assisted installer。
- SHA256：`13f98118e3102c6fa9e31a4bde5726b37b454529201709a15d0d368501596379`
- 安装验收时未签名（Authenticode: NotSigned），当时未上传、未创建 Release。后续公开预发布见 [发布记录](../release/0.1.0-preview.1.md)；安装包内容与本记录的 SHA256 保持一致。
- 固定主工作流：0.3.0，41 个文件；固定内置 Python：3.14.7 官方 Windows 嵌入式包。构建时校验两份 ZIP 的 SHA256，保留许可证；下载后的 Python 可执行文件签名检查为 Valid，签名者 Python Software Foundation。

## 验证结果

1. 类型检查通过；单元测试 38 项、集成测试 61 项、开发版 Electron 界面测试 7 项通过。
2. 安装版专项 Electron 测试 1 项通过：实际安装后的 exe 运行，新建中文路径项目、加载书本图标、保存草稿、退出再打开仍保留内容。运行时 PATH 只保留 Windows System32，没有 Node、pnpm、py 或系统 Python。
3. 最终安装包在项目内 `.tmp/windows-install-smoke/应用安装位置` 安装，返回 0。安装过程同样不依赖开发环境 PATH。默认桌面及开始菜单快捷方式存在，目标和图标指向安装后的 exe。
4. 安装钩子自动将主工作流装入独立的测试 CODEX_HOME；41 个文件再次逐一校验一致。未触碰用户真实 Skill 目录。
5. 安装版无窗口安装入口在遇到测试 Skill 修改时返回 2，人工修改内容保持原样。新装、相同版本复用、内容冲突、损坏资源、危险路径、清单外文件均有自动测试。
6. 内置 Python 能执行主工作流的项目结构验证脚本；安装版创建的合成项目结构校验通过、0 警告。不是学术质量验收。
7. 专用测试安装卸载返回 0；应用及测试快捷方式移除，外部测试项目与测试 Skill 保留。无用户论文参与测试。

合计 107 项自动测试通过。普通 `test:e2e` 中安装专项默认跳过；本次显式指定真实安装后的 exe，另行运行并通过，不把跳过算通过。

## 尚未验证或不包含

- 向导欢迎页、目录页的实际逐页视觉核对未完成：Windows 界面检查工具初始化时遇到 JSON BOM 错误。向导配置已编译通过，自选路径通过实际静默安装验证；不将这些替代成逐页视觉验收。
- 未在另一台全新 Windows 机器或虚拟机上测试；本机通过删减子进程 PATH 验证不依赖开发命令，不能据此声称所有机器兼容。
- 不包含 Codex 账号、MinerU 模型、附属 humanizer/mineru Skill 或研究数据。本次不调用模型、不消耗研究额度。
- 本记录不包含代码签名或跨机器分发验收；完整研究工作流的端到端学术验收、原版 PDF 阅读器等仍按原待办推进。
