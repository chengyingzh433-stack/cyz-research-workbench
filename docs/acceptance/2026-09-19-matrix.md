# 证据矩阵编辑验证

本批直接编辑工作流已有的 `03-文献证据矩阵.md`。首表 16 个标准列沿用工作流 0.3.0；界面展示文献 ID、主题、主要发现、证据状态、原文位置，选中条目后编辑全部字段。

六项解析测试覆盖标准表编辑、原文字节往返、周围说明及第二张表保留、未知列/脚注/单元格换行降为源码模式。四项文档集成测试覆盖初次纳管保存原版本、外部冲突、打开后的文件哈希约束及 UTF-8 BOM 原稿保留。

Electron 实测从已有文件添加合成条目、保存、重新打开，条目与人工备注均保留，成果计数同步更新。本机截图 `.tmp/desktop-matrix.png`，内容明确标为合成测试，不作为研究证据。

最新完整验证：类型检查、构建退出 0；单元 23/23、集成 43/43、Electron 4/4，共 70 项。真实恢复验证另见 `codex-recovery-probe.json`。

边界：目前录入的是用户整理的记录，不自动认定文献已核验，也不会自动填页码。复杂 Markdown 保留源码编辑，未实现通用富文本表格解析器。PDF 页码/块定位仍待接入。
